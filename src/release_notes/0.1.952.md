feature: Markdown files are linted as you edit them. Multiple top-level headings, a heading missing the space after its `#`, trailing whitespace and runs of blank lines are reported as squiggles in the editor and entries in the PROBLEMS panel, with no extension to install.
