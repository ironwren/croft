feature: colour literals show a swatch of the colour they name. A `#3b82f6`, an `rgb(59, 130, 246)` or an `hsl(217, 91%, 60%)` anywhere in the file is preceded by a block painted that colour, so a palette can be read at a glance instead of decoded in your head.
